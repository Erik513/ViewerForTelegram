namespace ViewerForTelegram.Data.Models;

/// <summary>
/// UI language offered by the ErikwnkWFUI library. English is the default.
/// The UI layer maps this to the library's own language enum - Data stays
/// free of a WinForms dependency.
/// </summary>
public enum DisplayLanguage
{
    English,
    German
}
